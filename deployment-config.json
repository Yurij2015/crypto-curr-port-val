[
  {
    "name": "Crypto Currency Portfolio",
    "ip": "138.201.196.61",
    "port": 2228,
    "username": "yurii",
    "path": "/home/yurii/crypto-curr-port-val",
    "beforeHooks": "echo 'Starting deployment for Crypto Portfolio...'",
    "afterHooks": "cd $RELEASE_PATH && docker compose -p crypto-app --env-file ../.env -f compose.yaml -f compose.prod.yaml build --no-cache --pull php php-scheduler && docker compose -p crypto-app --env-file ../.env -f compose.yaml -f compose.prod.yaml up -d --remove-orphans --no-build && docker exec crypto-app-php php bin/console doctrine:migrations:migrate --no-interaction",
    "backupImages": false
  }
]
